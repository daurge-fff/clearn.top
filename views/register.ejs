<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Register - Code & Learn</title>
  <link rel="stylesheet" href="/css/auth.css" />
  <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;800&family=Roboto:wght@400;700&display=swap" rel="stylesheet" />
</head>
<body>
    

<div class="auth-layout">
  <div class="auth-box animated">

    <div class="auth-side">
      <h2>“Every coder starts somewhere.”</h2>
      <p>— Start your journey with us today</p>
    </div>

    <div class="auth-form">
      <div class="auth-header">
        <h1>Create Account</h1>
      </div>

            <form action="/users/register" method="POST">
                <div class="form-group floating">
          <input type="text" name="name" id="name" required placeholder=" " value="<%= typeof name != 'undefined' ? name : '' %>" />
          <label for="name">Full Name</label>
        </div>

                <div class="form-group floating">
          <input type="email" name="email" id="email" required placeholder=" " value="<%= typeof email != 'undefined' ? email : '' %>" />
          <label for="email">Email <span class="required-asterisk">*</span></label>
        </div>

                <div class="form-group floating">
          <input type="text" name="contact" id="contact" placeholder=" " value="<%= typeof contact != 'undefined' ? contact : '' %>" />
          <label for="contact">Telegram / WhatsApp (optional)</label>
        </div>

                <div class="form-group floating">
          <input type="password" name="password" id="password" required placeholder=" " />
          <label for="password">Password</label>
        </div>

                <div class="form-group floating">
          <input type="password" name="password2" id="password2" required placeholder=" " />
          <label for="password2">Confirm Password</label>
        </div>

        <div class="form-group floating">
          <input type="text" name="referralCode" id="referralCode" placeholder=" " value="<%= typeof referralCode != 'undefined' ? referralCode : '' %>" />
          <label for="referralCode">Referral Code (optional)</label>
        </div>

                <input type="hidden" name="timeZone" id="timeZone" />
                <button type="submit" class="btn btn-primary">Create Account</button>
      </form>

            <div class="auth-divider">or</div>

            <a href="/users/auth/google" class="google-signin-button">
        <img src="/images/google.svg" alt="Google logo" width="18" height="18" />
        <span>Sign in with Google</span>
      </a>


            <p class="auth-footer">Already have an account? <a href="/users/login">Login</a></p>
    </div>
  </div>
</div>
    </div>
  </div>

  <script src="/js/global.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const timeZoneInput = document.getElementById('timeZone');
      if (timeZoneInput) {
        timeZoneInput.value = Intl.DateTimeFormat().resolvedOptions().timeZone;
      }
    });
  </script>
</body>
</html>
