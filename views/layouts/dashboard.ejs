<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Code & Learn</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;800&family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    
    <link href='https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.min.css' rel='stylesheet' />
</head>
<body class="dashboard-body" data-user-role="<%= user.role %>">
    <% if ((locals.success_msg && locals.success_msg.length > 0) || (locals.error_msg && locals.error_msg.length > 0) || (locals.error && locals.error.length > 0)) { %>
        <div class="flash-messages">
            <% if (locals.success_msg && locals.success_msg.length > 0) { %>
                <div class="flash-message flash-success"><%= success_msg %></div>
            <% } %>
            <% if (locals.error_msg && locals.error_msg.length > 0) { %>
                <div class="flash-message flash-error"><%= error_msg %></div>
            <% } %>
            <% if (locals.error && locals.error.length > 0) { %>
                <div class="flash-message flash-error"><%= error %></div>
            <% } %>
        </div>
    <% } %>

    <div class="dashboard-layout">
        <div id="sidebar-overlay" class="sidebar-overlay"></div>
        <aside class="dashboard-sidebar">
            <div>
                <div class="sidebar-header">
                    <a href="/" class="logo">C&L</a>
                    <div class="header-actions">
                        <button id="theme-toggle" aria-label="Toggle theme">🌙</button>
                        <a href="/users/logout" 
                           class="header-btn logout-btn" 
                           data-tooltip="Logout" 
                           onclick="return confirm('Are you sure you want to logout?');">
                            <i class="icon-logout"></i>
                        </a>
                    </div>
                </div>
                <nav class="sidebar-nav">
                    <a href="/dashboard" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'dashboard') ? 'active' : '' %>"><i class="icon-dashboard"></i><span>Dashboard</span></a>
                    
                    <% if(user.role === 'student') { %>
                        <a href="/dashboard/my-lessons" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'my-lessons') ? 'active' : '' %>"><i class="icon-calendar"></i><span>My Lessons</span></a>
                        <a href="/dashboard/progress" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'progress') ? 'active' : '' %>"><i class="icon-chart"></i><span>Progress</span></a>
                    <% } %>

                    <% if(user.role === 'admin') { %>
                        <a href="/dashboard/users" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'users') ? 'active' : '' %>"><i class="icon-users"></i><span>Users</span></a>
                        <a href="/dashboard/lessons" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'lessons') ? 'active' : '' %>"><i class="icon-list"></i><span>All Lessons</span></a>
                        <a href="/dashboard/analytics" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'analytics') ? 'active' : '' %>"><i class="icon-chart"></i><span>Analytics</span></a>
                        <a href="/dashboard/payments" class="nav-link <%= page_name === 'payments' ? 'active' : '' %>">
                            <i class="icon-credit-card"></i>
                            <span>Payments</span>
                        </a>
                    <% } %>

                     <% if(user.role === 'teacher') { %>
                        <a href="/dashboard/schedule" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'schedule') ? 'active' : '' %>"><i class="icon-calendar"></i><span>Schedule</span></a>
                        <a href="/dashboard/my-students" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'students') ? 'active' : '' %>"><i class="icon-users"></i><span>My Students</span></a>
                    <% } %>
                    <a href="/dashboard/settings" class="nav-link <%= (typeof page_name !== 'undefined' && page_name === 'settings') ? 'active' : '' %>"><i class="icon-settings"></i><span>Settings</span></a>
                </nav>
                
            </div>
            <div class="sidebar-footer">
                <div class="user-profile">
                    <div class="user-avatar">
                        <% if (user.emojiAvatar) { %>
                            <img src="https://emojicdn.elk.sh/<%= encodeURIComponent(user.emojiAvatar) %>?style=apple" alt="Avatar" class="emoji-avatar-img">
                        <% } else { %>
                            <span><%= user.name.charAt(0) %></span>
                        <% } %>
                    </div>
                    <div class="user-info">
                        <span class="user-name"><%= user.name %></span>
                        <span class="user-role"><%= user.role %></span>
                    </div>
                </div>
            </div>
        </aside>
        <div class="dashboard-main-wrapper">
            <main class="dashboard-main">
                <button id="sidebar-toggle" class="mobile-sidebar-toggle" data-tooltip="Toggle Menu">
                    <i class="icon-menu"></i>
                </button>
                <div class="dashboard-content">
                    <%- body %>
                </div>
            </main>
        </div>
    </div>

    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.min.js'></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/js/global.js"></script>
    
    <!-- Global Modal Structure -->
    <div id="lesson-modal-overlay" class="modal-overlay"></div>
    <div id="lesson-modal" class="modal-container">
        <div class="modal-header">
            <h2 id="modal-title">Lesson</h2>
            <button id="modal-close-btn" class="modal-close-btn">×</button>
        </div>
        <div class="modal-body">
            <form id="lesson-form" class="form-card" style="max-width: none; box-shadow: none; padding: 0; border: none;">
                <input type="hidden" id="lessonId" name="lessonId">
                <div id="modal-form-content">
                    <div class="spinner"></div>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary" id="modal-submit-btn">Save</button>
                    <button type="button" class="btn btn-secondary" id="modal-cancel-btn">Cancel</button>
                </div>
            </form>
        </div>
    </div>
    <div class="modal-overlay" id="admin-actions-overlay" style="display: none;"></div>
    <div class="modal-container" id="adjust-balance-modal" style="display: none;">
        <div class="modal-header">
            <h2>Adjust Balance for <span id="adjust-balance-username"></span></h2>
            <button class="modal-close-btn">×</button>
        </div>
        <div class="modal-body">
            <form id="adjust-balance-form">
                <input type="hidden" id="adjust-balance-userid" name="userId">
                <div class="form-group">
                    <label for="adjustment-amount">Amount</label>
                    <input type="number" id="adjustment-amount" name="adjustment" placeholder="e.g., 5 or -2" required>
                    <p class="form-hint">Use a positive number to add lessons, negative to subtract.</p>
                </div>
                <div class="form-group">
                    <label for="adjustment-reason">Reason</label>
                    <input type="text" id="adjustment-reason" name="reason" placeholder="e.g., Gift, Compensation, etc." required>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Save Adjustment</button>
                </div>
            </form>
        </div>
    </div>
    <div class="modal-container" id="link-payment-modal" style="display: none;">
        <div class="modal-header">
            <h2>Link Payment to User</h2>
            <button class="modal-close-btn">×</button>
        </div>
        <div class="modal-body">
            <form id="link-payment-form">
                <input type="hidden" id="link-payment-id" name="paymentId">
                <div class="form-group">
                    <label>Payment for: <code id="link-payment-identifier"></code></label>
                </div>
                <div class="form-group">
                    <label for="user-search-input">Search for a user (by name or email)</label>
                    <input type="search" id="user-search-input" placeholder="Start typing...">
                </div>
                <div id="user-search-results"></div>
            </form>
        </div>
    </div>

    <% if (typeof scripts !== 'undefined' && scripts) { %>
        <% scripts.forEach(script => { %>
            <script src="<%= script %>"></script>
        <% }) %>
    <% } %>
    
</body>
</html>