<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Code & Learn' %> - Dashboard</title>
    
    <link rel="stylesheet" href="/css/style.css?v=1.12">
    <link rel="stylesheet" href="/css/dashboard.css?v=1.0"> 
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;800&family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    
    <% if (typeof useCalendar !== 'undefined' && useCalendar) { %>
        <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.9/index.global.min.js'></script>
    <% } %>
</head>
<body dir="ltr">

    <header class="dashboard-header">
        <div class="container">
            <a href="/dashboard" class="logo">Code & Learn</a>
            <div class="header-controls">
                <span class="user-greeting">Welcome, <%= user.name %>!</span>
                <button id="theme-switcher" title="Switch theme"><img src="https://emojicdn.elk.sh/☀️" alt="Sun"></button>
                <a href="/users/logout" class="logout-button">Logout</a>
            </div>
        </div>
    </header>

    <main class="dashboard-main">
        <div class="container">
            <%- body %>
        </div>
    </main>
    
    <script src="/js/script.js?v=<%= Date.now() %>"></script>
    
    <% if (typeof useCalendar !== 'undefined' && useCalendar) { %>
        <script src="/js/dashboard.js?v=1.0"></script>
    <% } %>
</body>
</html>