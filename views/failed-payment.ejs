<!DOCTYPE html>
<html lang="<%= currentLang %>" dir="<%= lang.dir || 'ltr' %>" data-theme="dark">
 <head>
    <meta charset="UTF-8"><title><%= lang.paymentFailTitle %></title>
     <link rel="stylesheet" href="/css/style.css">
      <link href="https://fonts.googleapis.com/css2?family=Exo+2:wght@400;700&family=Montserrat:wght@300;400;600&display=swap&subset=cyrillic,latin-ext" rel="stylesheet">
    <style> body { display: flex; justify-content: center; align-items: center; min-height: 100vh; text-align: center; padding: 20px;}
        .payment-status { padding: 40px; border-radius: 12px; max-width: 600px; } .payment-status h1 { color: #e74c3c; font-family: var(--font-headings); margin-bottom: 15px; }
    </style>
</head>
<body>
    <div class="payment-status card-glass">
        <h1><%= lang.paymentFailTitle %></h1>
        <p><%= lang.paymentFailMsg %></p>
        <a href="/#tariffs?lang=<%= currentLang %>" class="cta-button" style="margin-top: 20px;"><%= lang.paymentRetry %></a>
    </div>
     <script src="/js/script.js"></script>
</body>
</html>