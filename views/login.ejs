<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Login - Code & Learn</title>
  <link rel="stylesheet" href="/css/dashboard.css" />
  <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;800&family=Roboto:wght@400;700&display=swap" rel="stylesheet" />
</head>
<body class="auth-layout">
    <% if ((locals.success_msg && locals.success_msg.length > 0) || (locals.error_msg && locals.error_msg.length > 0) || (locals.error && locals.error.length > 0)) { %>
        <div class="flash-messages">
            <% if (locals.success_msg && locals.success_msg.length > 0) { %>
                <div class="flash-message flash-success"><%= success_msg %></div>
            <% } %>
            <% if (locals.error_msg && locals.error_msg.length > 0) { %>
                 <div class="flash-message flash-error"><%= error_msg %></div>
            <% } %>
            <% if (locals.error && locals.error.length > 0) { %>
                 <div class="flash-message flash-error"><%= error %></div>
            <% } %>
        </div>
    <% } %>
  <div class="auth-theme-toggle">
    <button id="theme-toggle" aria-label="Toggle theme">🌙</button>
  </div>

  <div class="auth-box animated">
    <div class="auth-side">
      <h2>“Code is the future.”</h2>
      <p>— Join the journey and shape tomorrow</p>
    </div>

    <div class="auth-form">
      <div class="auth-header">
        <h1 class="fade">Welcome Back</h1>
        <p class="auth-subtitle fade">Log in to access your dashboard</p>
      </div>

      <form action="/users/login" method="POST" class="form-fields">
        <div class="form-group floating fade">
          <input type="email" name="email" id="email" required placeholder=" " />
          <label for="email">Email</label>
        </div>

        <div class="form-group floating fade">
          <input type="password" name="password" id="password" required placeholder=" " />
          <label for="password">Password</label>
        </div>

        <button type="submit" class="btn btn-primary fade">Login</button>
      </form>

      <div class="auth-divider fade">or</div>

      <a href="/users/auth/google" class="google-signin-button fade">
        <img src="/images/google.svg" alt="Google logo" width="18" height="18" />
        <span>Sign in with Google</span>
      </a>


      <p class="switch-auth fade">No account? <a href="/users/register">Register</a></p>
    </div>
  </div>

  <script src="/js/global.js"></script>
</body>
</html>
